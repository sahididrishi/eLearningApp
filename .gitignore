# Python-specific files
__pycache__/
*.pyc
*.pyd
.DS_Store  # macOS specific
*.swp  # Vim swap files
*~ # Backup files (emacs, vi)

# Django-specific files
*.log  # Log files
*.pot  # Message catalogs (if you're using internationalization)

# Database files (if you're using SQLite - consider moving to PostgreSQL or MySQL for production)
db.sqlite3
.sqlite-journal  # SQLite journal files

# Static files (if you're not managing them separately - ideally, these should be collected and served by a separate service in production)
static/
media/

# Virtual environment (very important!)
venv/
.venv/
env/
.env

# Secret keys and configuration files (never commit these!)
.env  # Environment variables
config.ini
settings.py  # If you have sensitive settings in your main settings file

# Temporary files and build artifacts
*.tmp
*.temp
build/
dist/
*.egg  # Python egg files
*.whl  # Python wheel files

# IDE-specific files (adjust as needed)
.idea/  # IntelliJ IDEA
.vscode/ # VS Code
.mypy_cache/ # Mypy cache

# OS-specific files
.DS_Store  # macOS Finder
Thumbs.db  # Windows Explorer

# Node.js and frontend dependencies (if applicable)
node_modules/
package-lock.json  # or yarn.lock

# Files generated by Docker (if you are using docker)
.dockerignore

# Files related to test coverage
.coverage
htmlcov/

# Other files you might want to exclude
.pytest_cache/ # pytest cache
*.bak # Backup files